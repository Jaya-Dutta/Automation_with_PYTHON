# Module 1.2 – WebDriver Introduction and Installation

## Objective

To understand Selenium WebDriver, browser drivers, WebDriver lifecycle, and browser initialization using Python.

## Topics Covered

* Selenium WebDriver
* Browser driver setup
* Chrome WebDriver
* Edge WebDriver
* Firefox WebDriver
* WebDriver lifecycle
* Browser launch and termination

## Implementation

Selenium WebDriver was used with Chrome, Edge, and Firefox to launch browsers, perform basic navigation, and manage the WebDriver lifecycle.

## Validation

The browser-specific scripts and WebDriver lifecycle script were executed successfully to verify browser initialization, navigation, and driver termination.

## Evidence

* Chrome WebDriver execution screenshot
* Edge WebDriver execution screenshot
* Firefox WebDriver execution screenshot
* WebDriver lifecycle execution screenshot

# Result

Selenium WebDriver setup and basic browser automation were successfully implemented and verified across multiple browsers.
